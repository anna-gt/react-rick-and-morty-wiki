import React from 'react';
import { useDispatch, useSelector } from 'react-redux';
import { getLocation } from '../components/actions/location';

const Location = () => {
	return (
		<div>
			Location
		</div>
	);
};

export default Location;